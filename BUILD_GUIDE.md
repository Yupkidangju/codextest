# BUILD_GUIDE

## 개요
본 프로젝트는 정적 웹 리소스만 사용하므로 별도 빌드가 필요하지 않다.

## 실행
1. 최신 브라우저(Chrome/Edge/Firefox/Safari)로 `index.html`을 연다.
2. 상단 `음악 시작` 버튼 클릭 후 오디오를 활성화한다.
3. 좌/우 버튼 또는 화살표 키로 페이지를 전환한다.

## 배포
- 정적 호스팅(GitHub Pages, Netlify, S3 등)에 파일 업로드만으로 배포 가능.
